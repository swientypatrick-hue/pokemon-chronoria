{
  "name": "e-pokemonwiki",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync-data": "node data-import/syncData.ts",
    "build-data": "node data-import/buildData.ts",
    "dev": "npm run sync-data && npm run build-data && astro dev",
    "build": "npm run sync-data && npm run build-data && astro build && pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.0.7"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
